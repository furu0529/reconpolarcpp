// 虚数単位
const std::complex<double> I(0.0,1.0);
// cosmological parameter
const double ndh=0.7010;
const double omegam=0.1408/0.7010/0.7010;
const double omegab=0.0205/0.7010/0.7010;
const double omegal=0.692;
const double sigma8=0.811;
const double rho0mMpc=2.7753*1e11*0.7010*0.7010;
const double deltac=1.686;
// map size
const int grid = 8;
const int bins = grid/2;
const double L = 30000; // Mpc

// シミュレーションで用いる静的変数（常に同じ値が入っている）
/*static double knorms[grid][grid][bins];
static std::vector<double>  kt;
static std::vector<double>  transf;
static std::complex<double> r_y2m[5][grid][grid][grid];
static std::complex<double> k_y2m[5][grid][grid][grid];
static std::complex<double> r_box_harm[5][grid][grid][grid];
static std::complex<double> k_box_harm[5][grid][grid][grid];*/

// 何度も入れ回すやつ
//std::complex<double> kinitial_true[grid][grid][bins];

// trueBOX （常に同じ値が入っている）
//static std::complex<double> k_box_true[grid][grid][grid];
//static std::complex<double> r_box_true[grid][grid][grid];
//static std::complex<double> a2m_true[5][grid][grid][grid];
//static std::complex<double> QU_true[grid][grid][grid];
// fitting box
//static std::complex<double> kinitial_fit[grid][grid][grid];
//static std::complex<double> k_box_fit[grid][grid][bins];
//static std::complex<double> r_box_fit[grid][grid][grid];
//static std::complex<double> QU_fit[grid][grid][grid];
